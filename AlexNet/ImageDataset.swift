import Foundation
import TensorFlow

//let imageDataset = Dataset(elements: <#T##_#>)

struct ImageDataset {
//    let labels:[String]
    
    init(imageDirectory: URL, imageSize: (Int, Int)) {
        // Parse the directory, gather subdirectories
        // Assign subdirectories to label array
        // Iterate over images in subdirectory
        // Load image
        // Resize image
        // Pull into BGR colorspace
        // Create image tensor
        // Add to label array
    }
}
